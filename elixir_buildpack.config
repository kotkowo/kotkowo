# Erlang version
erlang_version=24.3.4.6

# Elixir version
elixir_version=1.14.1

# Always rebuild from scratch on every deploy?
always_rebuild=false

# Export heroku config vars
config_vars_to_export=(DATABASE_URL)
